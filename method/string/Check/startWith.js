const str = "farid"

// string.startsWith(searchString, position?) → check beginning of string
// searchString → the text you want to check
//position → index where the check begins
// (default = 0)
// string.startsWith(searchString, position)

console.log(str.startsWith("farid",0))
console.log(str.startsWith("rid",2))
