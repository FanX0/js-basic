const str = "farid"

// string.slice(start, end?) → extract substring without modifying original
// start (inclusive), end (exclusive). Negative indexes count from end.
console.log(str.slice(2,5)) //rid